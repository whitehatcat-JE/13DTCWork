/**
 * Stores the card pool, runs the instances and returns the probabilities
 * 
 * @author Jayden & co
 * @version 25/03/21
 */

public class simulation {
    final static int POOLSIZE = 15;
    static var pool = new MasterCard[POOLSIZE];

    public static void generatePool() {
        var cat = new cat();
        pool.push(cat);


    }
    public static void () {
        
    }
}